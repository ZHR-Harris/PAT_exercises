#include <iostream>
#include <stdio.h>
using namespace std;
int main()
{
    // #ifdef ONLINE_JUDGE
    // #else
    freopen("1.txt", "r", stdin);
    // #endif
    // int n, sum = 0;
    // cin >> n;
    // while (n != 0) {
    //     sum += n % 10;
    //     n /= 10;
    // }
    // cout << sum << endl;

    const int maxn = 10;
    char str
    fclose(stdin);
    return 0;
}
