#include <iostream>
#include <vector>
using namespace std;
int book[201], a[10001] = {1}, dp[10001];
int main() {
  for (int i = 0; i < 100; i++) {
    std::cout << a[i] << std::endl;
  }

  return 0;
}